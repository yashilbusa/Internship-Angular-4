import { DateTimeConverterDirective } from './date-time-converter.directive';

describe('DateTimeConverterDirective', () => {
  it('should create an instance', () => {
    const directive = new DateTimeConverterDirective();
    expect(directive).toBeTruthy();
  });
});
