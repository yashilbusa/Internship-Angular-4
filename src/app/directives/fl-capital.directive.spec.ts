import { FlCapitalDirective } from './fl-capital.directive';

describe('FlCapitalDirective', () => {
  it('should create an instance', () => {
    const directive = new FlCapitalDirective();
    expect(directive).toBeTruthy();
  });
});
