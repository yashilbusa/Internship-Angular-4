import { RmDecimalPointDirective } from './rm-decimal-point.directive';

describe('RmDecimalPointDirective', () => {
  it('should create an instance', () => {
    const directive = new RmDecimalPointDirective();
    expect(directive).toBeTruthy();
  });
});
